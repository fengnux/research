# 2026-05-17 實驗日誌

## 目標

執行 [Lab 04d](../lab-04d-drift-detection.md)：新增 scheduled drift detection。

---

## 進度紀錄

### Phase A — WIF stack：新增 drift SA（本機 apply）

- [x] 修改 `stacks/ci/github-actions-wif/main.tf`：新增 `github-actions-tofu-drift` SA、WIF binding（`ref:refs/heads/main`）、3 個 read-only IAM roles
- [x] 修改 `outputs.tf`：新增 `drift_service_account_email`
- [x] commit [3c5cfbd](https://github.com/fengnux/tofu-terramate-lab/commit/3c5cfbd)
- [x] `tofu apply`：5 added（SA 1 + WIF binding 1 + IAM roles 3）

### Phase B — 新增 drift detection workflow

- [x] 建立 `.github/workflows/drift.yml`（schedule daily 02:00 UTC + workflow_dispatch）
- [x] commit [ae7a16d](https://github.com/fengnux/tofu-terramate-lab/commit/ae7a16d)

---

## 踩坑

### 坑一：IAM propagation（首次 `workflow_dispatch`）

第一次觸發立即 403：`Permission 'iam.serviceAccounts.getAccessToken' denied`。

原因：WIF IAM binding 剛 apply 完不到幾分鐘，GCP IAM 尚未全球同步。

解法：重新觸發即可（第二次成功通過 auth）。

### 坑二：provider download timeout（第二次觸發）

第一次 IAM 問題消失後，改成 provider 下載 timeout：
```
net/http: request canceled (Client.Timeout exceeded while awaiting headers)
```

原因：drift job 同時對所有 stack 跑 `tofu init`，下載 google provider SHA256SUMS 時 github.com 連線逾時。PR plan job 只對 changed stacks 跑，通常只有 1 個 stack，比較不容易觸發。

解法：加 `actions/cache@v4` + `TF_PLUGIN_CACHE_DIR` 快取 provider 二進位。

commit [be36913](https://github.com/fengnux/tofu-terramate-lab/commit/be36913)

### 坑三：`TF_PLUGIN_CACHE_DIR: ~/.terraform.d/plugin-cache` 字面 `~`

加了 plugin cache 後，下一次觸發在 Drift check 步驟報 `Error: repository has untracked files`。

根本原因：YAML `env` block 的值不經 shell 展開，`~` 是字面值。OpenTofu 用 Go 的 `os.MkdirAll` 建立 `~/` 時，把 `~` 當相對路徑，在各 stack 目錄下建了 `~/` 資料夾——不在 `.gitignore` 裡，Terramate git safeguard 直接 fail。

解法：從 `env` block 移除 `TF_PLUGIN_CACHE_DIR`，改在 "Create plugin cache dir" step 透過 `GITHUB_ENV` 設定：
```bash
echo "TF_PLUGIN_CACHE_DIR=$HOME/.terraform.d/plugin-cache" >> "$GITHUB_ENV"
```

commit [f84da3f](https://github.com/fengnux/tofu-terramate-lab/commit/f84da3f)

### 坑四：`--label drift` label 不存在

第三次觸發時 drift check 通過（有偵測到東西），但 `gh issue create --label drift` 失敗：
```
could not add label: 'drift' not found
```

解法：issue 建立前先用 `gh label create ... 2>/dev/null || true` 確保 label 存在。

commit [afb0933](https://github.com/fengnux/tofu-terramate-lab/commit/afb0933)

### 坑五：舊 drift check 實作 exit code 被 printf 蓋掉

run 25977776828 顯示「無 drift、全綠」，但 dev/vm 實際上有 drift（VM 已 destroy，config 仍定義 VM）。

根本原因：舊實作用 `sh -c '... && tofu plan -detailed-exitcode; printf "\`\`\`"'`，`sh -c` 的 exit code 是最後一個指令 `printf`（exit 0），把 `tofu plan` 的 exit 2 完全蓋掉。`terramate run` 因此看到 exit 0，drift step 標記 success，issue 從未建立。

解法：放棄 `terramate run` 包法，改為手動 `while` 迴圈逐 stack 執行，用 `|| plan_exit=$?` 明確捕捉 exit code：

```bash
plan_exit=0
(cd "$GITHUB_WORKSPACE/$stack" && tofu plan -detailed-exitcode ...) >> "$report" 2>&1 || plan_exit=$?
case $plan_exit in
  0) ;;
  2) drift_found=true ;;
  *) error_found=true ;;
esac
```

同時拆成兩個 output：`drift_found`（exit 2）和 `error_found`（exit 1），後續 steps 分別用 `steps.drift.outputs.drift_found == 'true'` 和 `steps.drift.outputs.error_found == 'true'` 判斷。

commits：[6f01789](https://github.com/fengnux/tofu-terramate-lab/commit/6f01789)、[656eed0](https://github.com/fengnux/tofu-terramate-lab/commit/656eed0)

### 坑六：`set -e` 與 exit 2 提前退出

改用 `||` 模式後第一次跑，drift check step 以 exit 2 結束，後續 steps 全被跳過（issue 未建立）。

原因：GitHub Actions bash 預設 `set -e`；`(subshell)` 回傳 2 時，即使下一行是 `plan_exit=$?`，bash 已在賦值前退出。加上 `|| plan_exit=$?` 讓 `||` 右側吸收 non-zero exit，`set -e` 不再觸發。

---

## 驗收結果

最終驗證（[run 25978343972](https://github.com/fengnux/tofu-terramate-lab/actions/runs/25978343972)）：

| 項目 | 結果 |
|------|------|
| Drift SA WIF auth（`workflow_dispatch` → `ref:refs/heads/main` subject） | ✅ |
| Plugin cache 正常使用 | ✅ |
| exit 2（drift）→ 開 Issue，exit 1（error）→ 不開 Issue、workflow fail | ✅ |
| dev/vm 正確偵測到 drift（`Plan: 1 to add`） | ✅ |
| dev/apis、dev/network 無 drift | ✅ |
| Issue deduplication（已有 open issue → 留言而非新開） | ✅（設計驗證） |
| `workflow_dispatch` 手動觸發可用 | ✅ |
| Schedule trigger 暫時停用（dev/vm drift 為預期狀態） | ✅ |

### dev/vm drift 說明

`stacks/dev/vm` 在 Lab 03c 執行 `tofu destroy` 後 VM 已從 GCP 刪除，但 `main.tf` 保留供實驗參考。State 記錄「無資源」、Config 定義「要有 VM」，drift detection 正確偵測到差距（`Plan: 1 to add`）。

處置：Issue #9 以「預期 drift，非異常」關閉。Schedule trigger 暫時停用，待 dev/vm 定位確定後再決定是否排除該 stack 或移除 config。

---

## SA 架構整理（Lab 04d 後）

| SA | WIF subject binding | 用途 |
|----|---------------------|------|
| `github-actions-tofu` | `ref:refs/heads/main` | apply（main push） |
| `github-actions-tofu-plan` | `pull_request` | plan（PR） |
| `github-actions-tofu-drift` | `ref:refs/heads/main` | drift detection（schedule / workflow_dispatch） |

---

## 後續

- [x] 升級 actions 至 Node.js 24 runtime（checkout v6、cache v5、setup-opentofu v2）— commit [dca9e86](https://github.com/fengnux/tofu-terramate-lab/commit/dca9e86)
- [x] Lab 04e：Trivy IaC 安全掃描（見下方）
- [ ] Lab 04g：Terramate Cloud 整合
- [ ] Dev/vm drift 定位後重新啟用 schedule trigger

---

# Lab 04e — Trivy IaC 安全掃描

## 目標

執行 [Lab 04e](../lab-04e-security-scan.md)：PR 觸發掃變更 stacks（HIGH/CRITICAL fail）、schedule 全 repo 掃描；同時把所有 workflow 第三方 action 改 commit SHA pinning。

## 工具選型

- Trivy v0.70.0（事件後乾淨版；2026-03-19 供應鏈攻擊 [CVE-2026-33634](https://github.com/aquasecurity/trivy/security/advisories/GHSA-69fq-xp46-6x23)，CVSS 9.4）
- 用 `curl install.sh` 安裝 trivy binary（install.sh pin v0.70.0 release commit SHA `8a3177ae`），不依賴 `trivy-action`，攻擊面收斂至 trivy GitHub repo 單一信任源

## 進度紀錄

### Phase A — 查 commit SHA + Trivy 乾淨版本

| Action | SHA | 對應版本 |
|--------|-----|---------|
| actions/checkout | de0fac2e4500dabe0009e67214ff5f5447ce83dd | v6.0.2 |
| actions/cache | 27d5ce7f107fe9357f9df03efb73ab90386fccae | v5.0.5 |
| terramate-io/terramate-action | 1cc78a548efefaab7762f4d06f51aa6de06ce201 | v3.3.0 |
| opentofu/setup-opentofu | fc711fa910b93cba0f3fbecaafc9f42fd0c411cb | v2.0.0 |
| google-github-actions/auth | 7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 | v3.0.0 |
| marocchino/sticky-pull-request-comment | 773744901bac0e8cbb5a0dc842800d45e9b2b405 | v2.9.4 |
| github/codeql-action/upload-sarif | 9e0d7b8d25671d64c341c19c0152d693099fb5ba | v4.35.5 |
| trivy binary install.sh | 8a3177aedf7ee0864920eb1852eef031cd3742b8 | trivy v0.70.0 release |

### Phase B–D — opentofu.yml SHA pinning + 新增 security-scan-pr / security-scan.yml + .trivyignore

- 主 PR [#10](https://github.com/fengnux/tofu-terramate-lab/pull/10)（merged `eff0975`）：opentofu.yml 16 處 + drift.yml 5 處 = 21 處 action 改 SHA pin、新增 `security-scan-pr` job（並行 plan、僅 PR 觸發、只掃 `terramate list --changed`、HIGH/CRITICAL→fail、MEDIUM/LOW→annotate）、新增 security-scan.yml（schedule 先註解、`workflow_dispatch` 先驗）、`.trivyignore` 骨架

### Phase E — 驗證

#### E.1 主 PR PR Checks 全綠

✅ Plan、Trivy IaC scan（changed=0 → skip）、generate-check、detect-foundational

#### E.2 第一次 schedule scan 兩個 bug → fix PR

第一次 `gh workflow run security-scan.yml`（run [25980059473](https://github.com/fengnux/tofu-terramate-lab/actions/runs/25980059473)）fail：

| Bug | 原因 | 修正 |
|-----|------|------|
| `unknown flag: --ignore-unfixed` | 這 flag 是 `trivy image`（CVE scan）用的，`trivy config` 不支援 | 移除 flag |
| CodeQL Action v3 deprecation warning | December 2026 過期 | 順手升 v3.35.5 → v4.35.5 |

fix PR [#11](https://github.com/fengnux/tofu-terramate-lab/pull/11)（merged `9d115bc`）。

#### E.2 重跑 schedule scan → 乾淨

Run [25980222042](https://github.com/fengnux/tofu-terramate-lab/actions/runs/25980222042)：

- 掃描 5 個非 foundational stacks（bootstrap, ci/github-actions-wif, dev/apis, dev/network, dev/vm）
- **HIGH/CRITICAL finding 數**：**0**
- SARIF 上傳成功（analysis id 1258913520，category `trivy-iac-full`）
- 0 Issue 開啟（design 行為：finding=0 → 不開）
- ✅ `.trivyignore` 不需 baseline ignore

#### E.3 測試違規 PR（draft，驗完 close）

PR [#12](https://github.com/fengnux/tofu-terramate-lab/pull/12)（**closed, not merged**）：

**第一次嘗試**：`iap_source_range = "0.0.0.0/0"` → Trivy 把 SSH 開放 0.0.0.0/0 列為 **MEDIUM**（不是 HIGH），fail path 沒觸發；另外 tofu fmt -check 抓到我 comment 多加的空格。學到：嚴重度分級主觀，「公開 port 22」在 Trivy 是 MEDIUM。

**第二次嘗試**：改加 `google_storage_bucket_iam_member` 給 `allUsers`，並還原 IAP + 修 fmt。結果：

| 驗收項目 | 結果 |
|---------|------|
| 規則 | **GCP-0001** "Bucket allows public access" |
| 嚴重度 | **HIGH** |
| 觸發位置 | `main.tf:90` |
| `security-scan-pr` job | ❌ fail（gate 正確擋下） |
| Plan changed stacks | ✅ pass（互不阻塞） |
| SARIF 上傳 | ✅ Successfully uploaded（analysis id 1258940229, results_count=1） |
| Code Scanning alert | ✅ `state: open, severity: high, rule_id: GCP-0001`（filter `pr:12` 可見，預設 `branch:main` 看不到） |

PR 12 closed 不 merge，branch 刪除。

#### E.4 Phase F — 啟用 schedule cron

PR [#13](https://github.com/fengnux/tofu-terramate-lab/pull/13)（merged `a33f183`）：取消 `security-scan.yml` 的 `schedule: cron: '0 3 * * *'` 註解。每日 03:00 UTC（台灣 11:00）自動跑全 repo 掃描。錯開 drift 02:00。

---

## 驗收結果

| 項目 | 結果 |
|------|------|
| 21 處 third-party action 全 SHA-pinned + 版本 comment | ✅ |
| Trivy 走 install.sh + SHA pin，不依賴 trivy-action | ✅ |
| PR `security-scan-pr` job 並行 `plan`，僅 PR 觸發 | ✅ |
| PR scan 範圍 = `terramate list --changed --no-tags foundational`（無變更時 skip） | ✅ |
| Schedule scan 全 repo HIGH/CRITICAL → 開 dedup Issue + fail | ✅（設計，目前 finding=0 沒實際開過） |
| HIGH/CRITICAL violation → PR fail（GCP-0001 驗證） | ✅ |
| MEDIUM/LOW → annotate-only，不阻擋 | ✅ |
| SARIF 上傳 Code Scanning，兩 category 分開（pr / full） | ✅ |
| `.trivyignore` 不需 baseline（目前 0 HIGH/CRITICAL） | ✅ |
| CodeQL Action 升 v4 避開 Dec 2026 deprecation | ✅ |

---

## 學到的事

### 1. Trivy 供應鏈攻擊事件影響使用模式

2026-03 trivy-action 76 個 tag 被 force-push、setup-trivy 全 tag 竄改、trivy v0.69.4-6 為惡意版本。**緩解措施**：所有 action 用 commit SHA 而非 tag、不依賴中間 action wrapper、直接 install + SHA pin binary。

### 2. trivy config 與 trivy image 是不同 sub-command

- `trivy config`：IaC misconfig 掃描，不支援 `--ignore-unfixed`（fix PR 才發現）
- `trivy image`：CVE 漏洞掃描，`--ignore-unfixed` 在這裡才有意義（不顯示無修補版的 CVE）

### 3. Trivy 嚴重度分級有自己的觀點

「Firewall 開 SSH 給 0.0.0.0/0」在很多 audit framework 是 HIGH，但 Trivy 是 **MEDIUM**。如果要把這類規則升級為 fail，需要自己加 `--severity LOW,MEDIUM,HIGH,CRITICAL` 並用 `.trivyignore` 把可接受的 MEDIUM 排除。

### 4. Code Scanning UI 預設 filter 是 `branch:main`

PR scan 產生的 alert 在 `refs/pull/N/merge` ref，不會出現在 main filter。要看 PR alert：搜尋框輸入 `pr:N`，或從 PR conversation/Files Changed tab 進入。PR closed 不 merge 後 alert 自動 dismiss。

### 5. PR-first workflow 正式落地

本 lab 起所有 stack/workflow 變更走 PR：feature branch → CI plan + scan → review → merge → CI apply。Foundational stack 還是本機 apply 但 .tf 也走 PR。記入 memory `feedback_pr_first_workflow.md`。

---

## SA 架構不變

| SA | WIF subject binding | 用途 |
|----|---------------------|------|
| `github-actions-tofu` | `ref:refs/heads/main` | apply（main push） |
| `github-actions-tofu-plan` | `pull_request` | plan（PR） |
| `github-actions-tofu-drift` | `ref:refs/heads/main` | drift detection（schedule / workflow_dispatch） |

Lab 04e 新增的 security-scan job 用 `GITHUB_TOKEN`（contents:read + security-events:write）即可，**不需要 GCP SA**（純靜態掃 .tf 檔，不打 GCP API）。

---

## 後續

- [x] Lab 04h：Dependabot 自動升級 action SHA pinning（見下方）
- [ ] Lab 04g：Terramate Cloud 整合（之後單獨進行）
- [ ] Dev/vm drift 處置決定後啟用 drift schedule
- [ ] 評估是否把 MEDIUM 也納入 fail（policy 討論）
- [ ] 評估 trivy secret scan（會掃 git history，需評估時間成本）

---

# Lab 04h — Dependabot 自動更新 Action SHA

## 目標

執行 [Lab 04h](../lab-04h-dependabot.md)：Lab 04e 把所有 third-party action SHA-pinned 後，補上「自動升級層」— Dependabot 每週掃描，新版時集中開 1 個 PR。

## 進度紀錄

### Phase A — `.github/dependabot.yml`

| 欄位 | 值 |
|------|----|
| ecosystem | `github-actions` |
| schedule | weekly, Monday 06:00 UTC（台灣 14:00） |
| group | `all-actions` patterns `*`（全部合 1 PR） |
| labels | `dependencies`, `github-actions` |
| auto-merge | ❌（強制人工 review changelog） |
| 不追蹤 | `TRIVY_VERSION` / `TOFU_VERSION` / `TERRAMATE_VERSION`（env var，Dependabot 不支援）|

主 PR [#14](https://github.com/fengnux/tofu-terramate-lab/pull/14)（merged [41804ff](https://github.com/fengnux/tofu-terramate-lab/commit/41804ff)）。

### Phase B — Dependabot 立刻識別 → 開 PR 15

Merge 後 ~5 分鐘 Dependabot 第一次 scan，識別 7 個 third-party action 並發現 `marocchino/sticky-pull-request-comment` v2.9.4 → v3.0.4 可升。

PR [#15](https://github.com/fengnux/tofu-terramate-lab/pull/15)（merged [d523e49](https://github.com/fengnux/tofu-terramate-lab/commit/d523e49)）：

| 驗收 | 結果 |
|------|------|
| 自動產生 SHA + 版本 comment 同時更新 | ✅ `773744...` → `0ea0beb...`、`# v2.9.4` → `# v3.0.4` |
| `all-actions` group 合併 | ✅（雖然這次只有 1 項，但 PR title `the all-actions group` 證明 group 生效） |
| PR 觸發完整 CI | ✅ 全綠 |
| v3.0.0 changelog 確認 | ✅ 只有 "Update node to 24"，無 API breaking change；與我們 Lab 04d followup 一致 |

### Phase C — 修 title duplicate scope bug

PR 15 title 出現 `chore(deps)(deps)`：原本 `dependabot.yml` 同時設了 `prefix: "chore(deps)"` 與 `include: scope`，後者會自動加 `(deps)` scope，兩者疊加。

fix PR [#16](https://github.com/fengnux/tofu-terramate-lab/pull/16)（merged [8f79f6d](https://github.com/fengnux/tofu-terramate-lab/commit/8f79f6d)）：

```diff
- prefix: "chore(deps)"
+ prefix: chore
  include: scope
```

之後 Dependabot PR title 會是乾淨的 `chore(deps): bump ...`。

---

## 驗收結果

| 項目 | 結果 |
|------|------|
| `.github/dependabot.yml` 部署成功 | ✅ |
| Dependabot 識別 7 個 third-party action | ✅（PR 15 證明） |
| Schedule = weekly Monday 06:00 UTC | ✅ |
| 全部合併 1 PR（group `all-actions`） | ✅ |
| SHA + 版本 comment 同步更新 | ✅ |
| commit prefix `chore(deps): ...`（修完 title bug 後） | ✅ |
| 無 auto-merge | ✅（design） |
| Trivy / Tofu / Terramate env var 不被追蹤 | ✅（design，預期手動升級） |

---

## 學到的事

### 1. Dependabot for github-actions 即裝即用

不像 trivy-action 等 wrapper action 需要 SHA pin、信任源評估，Dependabot 是 GitHub 內建服務，merge 後幾分鐘就動。零安裝、零外部 GitHub App 安裝攻擊面。

### 2. `commit-message.prefix` + `include: scope` 不能重疊

如果 prefix 已含 scope（如 `"chore(deps)"`），就不要再加 `include: scope` 否則會疊加成 `(deps)(deps)`。正確做法：`prefix: chore` + `include: scope`。

### 3. Major bump 不一定 = breaking change

`sticky-pull-request-comment v2 → v3` 的 v3.0.0 changelog 只有 "Update node to 24"，純內部 runtime 升級。**仍要看 changelog 才能知道**，這就是為何不設 auto-merge：要人類眼睛確認 release notes。

### 4. `gh api repos/.../dependabot/alerts` 不是 dependency list

Dependabot 有兩個產品：
- **Dependabot version updates**（我們這次用的）：定期升版本，API endpoint `repos/{owner}/{repo}/network/updates` 或網頁 `/network/updates`
- **Dependabot security alerts**：偵測已知 CVE，API `repos/{owner}/{repo}/dependabot/alerts`

兩者不同。我們現在第一個的證據是 PR 15 自動出現；第二個目前 alerts = 0。

---

## 後續

- [ ] 下個週一（2026-05-18）06:00 UTC 觀察首個由 schedule 觸發的 Dependabot PR
- [ ] 若 PR 太頻繁（每週都來），考慮 `interval: monthly`
- [ ] 若未來想要 auto-merge：建立 `dependabot-auto-merge.yml`，只 auto-merge patch 級別 + CI 全綠 + 排除 trivy-action / setup-trivy
- [ ] 評估 Renovate `regexManagers` 以納入 trivy / tofu / terramate env var 追蹤（目前不需要）

---

# Lab 05a：VPC 抽 module + 階層化 globals

## 目標

執行 [Lab 05a](../lab-05a-vpc-module-hierarchical-globals.md)：把 `stacks/dev/network/` 5 個 GCP resource 抽出為 `_modules/network/` reusable module，用 `generate_hcl` + 階層化 globals 動態餵參數，moved blocks 保 state 連續（0 雲端變更）。

## 進度紀錄

### Phase A — 建 `_modules/network/` 4 檔案
- `variables.tf` 8 變數（name_prefix、region、CIDR×3、range names、iap_source_range）
- `versions.tf` required_providers `google >= 7.0`（彈性，stack 端用 `~> 7.31.0` pin）
- `main.tf` 5 resource 全參數化；name_prefix="dev" 展開後與原值完全一致
- `outputs.tf` 6 outputs 同原 schema
- Snapshot baseline：`tofu state list > /tmp/network-state-before.txt`、`gcloud compute networks describe dev-vpc --project=research-lab-495809 > /tmp/network-vpc-before.txt`（id `2108969596956345683`）

### Phase B — 階層化 globals
- **意外發現**：`stacks/dev/globals.tm.hcl` 已存在且定義 `globals "env" { name, project }`，與 runbook 草案的 `env.tm.hcl` 衝突（attribute redeclared）
- 整合方式：刪掉新建的 `env.tm.hcl`，把 `name_prefix = "dev"` 加進現有 `globals.tm.hcl`
- 新增 `stacks/dev/network/network.tm.hcl` 放 CIDR 等 network globals，`pods_range_name` 用 `"${global.env.name_prefix}-pods"` 展開（globals 跨檔引用）
- 用 `terramate debug show globals` 驗證 scope：dev/network 看到 env+network，bootstrap 看不到（隔離正確）

### Phase C — stack 改寫
- 新增 `generate.tm.hcl` 動態生成 `_module_vpc.tf`
- 新增 `moved.tf` 5 個 moved block
- `outputs.tf` 改 `module.vpc.*`
- 刪 `main.tf`
- Local `tofu plan`：**`0 to add, 0 to change, 0 to destroy.`** + 5 個 `# X has moved to Y` 預覽 ✓

### Phase D — PR + CI plan
- [PR #17](https://github.com/fengnux/tofu-terramate-lab/pull/17) merged 為 commit `5c8b704`
- CI plan 同樣 0 changes ✓

### Phase E — 第一次 apply 失敗（WIF 踩坑）
- Apply [run 25990360805](https://github.com/fengnux/tofu-terramate-lab/actions/runs/25990360805) 在 `tofu init` 階段就掛
- 錯誤：`Permission 'iam.serviceAccounts.getAccessToken' denied`
- 詳見下方「踩坑」

### Phase E retry — apply success
- 修完 WIF 後新 PR [#19](https://github.com/fengnux/tofu-terramate-lab/pull/19) 觸發新 apply [run 25991083540](https://github.com/fengnux/tofu-terramate-lab/actions/runs/25991083540) success
- `diff /tmp/network-state-{before,after}.txt`：5 個 address 全加 `module.vpc.` 前綴 ✓
- `diff /tmp/network-vpc-{before,after}.txt`：**無差異**（id + creationTimestamp 一致，雲端未重建）✓
- `tofu output` 6 個值與 refactor 前一致 ✓

## 踩坑

### 坑一：Lab 04a 的 `environment: production` 讓 OIDC sub claim 改型，但 WIF binding 沒對齊

**症狀**：apply job init 階段 GCS backend 拿 token 403 `iam.serviceAccounts.getAccessToken`。Plan job 反而正常。

**Root cause**：
- Lab 04a 在 apply job 加 `environment: production`（approval gate）
- GitHub OIDC 在進入 environment 時 sub claim 從 `repo:.../ref:refs/heads/main` 變 `repo:.../environment:production`
- 但 `github_actions_tofu` SA 的 `roles/iam.workloadIdentityUser` binding 還是寫舊 ref 形式
- Plan job 沒 environment、走 `pull_request` subject 對應 `wif_plan` binding，所以沒事
- Lab 04a–04h 之間沒有 changed-stack apply（都只動 `.github/`），這個 bug 一直藏到 Lab 05a 才暴露

**修法**：[PR #18](https://github.com/fengnux/tofu-terramate-lab/pull/18) 把 binding member 從 `subject/repo:.../ref:refs/heads/main` 改成 `subject/repo:.../environment:production`。`ci/github-actions-wif` 是 foundational stack（CI 不會碰），本機個人 ADC 先 apply、再 push PR 補 source-of-truth。

### 坑二：runbook 設計時沒探完 `.tm.hcl` 既有檔

`stacks/dev/globals.tm.hcl` 已存在但 runbook 草案沒注意（我只看了 root config + generate + stack.tm.hcl）。Phase B 第一次 `terramate debug show globals` 才報 `attribute "name" redeclared`。

教訓：寫 runbook 前 `find . -name '*.tm.hcl'` 全列一次，避免在現有 globals 旁邊「新增」造成衝突。

### 坑三：Stale workflow run re-run 不可靠

WIF 修完想 `gh run rerun 25990360805 --failed`，但該 run 對應的 commit (`5c8b704`) 已不是 main head（後續 merge 了 #18），re-run 行為不一致。改成新 PR 加註解觸發 change detection，乾淨。

## 驗收結果

| 項目 | 結果 |
|------|------|
| `_modules/network/` 4 檔案建立 | ✅ |
| Hierarchical globals scope 隔離（dev 子樹 vs bootstrap）| ✅ 用 `terramate debug show globals` 驗證 |
| Local + CI plan 0 changes | ✅ |
| Apply success（含 WIF fix）| ✅ Run 25991083540 |
| State address 全部加 `module.vpc.` 前綴 | ✅ |
| 雲端 VPC id + creationTimestamp 不變 | ✅ |
| 6 outputs 值不變 | ✅ |

## Multi-env 範本（Phase F：value proof）

未來新增 staging 環境只需要：

```
+ stacks/staging/globals.tm.hcl       # env.name="staging", env.name_prefix="staging"
+ stacks/staging/apis/stack.tm.hcl    # 複製 dev/apis 改 id（純 metadata）
+ stacks/staging/network/stack.tm.hcl
+ stacks/staging/network/network.tm.hcl   # 自己的 CIDR（避免與 dev 重疊）
```

**零 `.tf` 檔複製**。`_module_vpc.tf` 由 `generate_hcl` 自動產生，module 共用同一份實作。這是「設定 re-use」的價值證明。

如果未來真的開 staging GCP project，還需要：
- bootstrap state bucket（per env 或共用都行）
- staging 專屬 WIF SA + binding（per env approval gate）
- 修 `generate.tm.hcl` 的 backend prefix 邏輯支援跨 env state 路徑

## 學到的事

### 1. `moved` block 是 state refactor 的標配，不要再用 `terraform state mv`

- 聲明式、PR 階段就能 review 對應關係
- state 改寫實際在 `apply` 時發生（plan 只是 preview）
- 用 `tofu state list` + `gcloud describe` 前後 diff 是最直接的驗收證據
- 完成後可選擇保留 `moved.tf` 一段時間再清，平衡可追溯性與檔案乾淨度

### 2. `generate_hcl` 比靜態 `.tf` + locals 橋接更貼 Terramate-native

整個 `module "vpc" {}` block 由 globals 展開，未來新 env 真的零 `.tf` 撰寫。代價是生成檔（`_module_vpc.tf`）需 commit 進 repo（per Terramate 慣例），review 時要連同看一眼。

### 3. Globals 跨檔引用支援 lazy 計算

`stacks/dev/network/network.tm.hcl` 內可以寫 `pods_range_name = "${global.env.name_prefix}-pods"`，引用同層 `stacks/dev/globals.tm.hcl` 的值。檔案順序無關（Terramate evaluate 是 lazy）。

### 4. Approval gate 改 OIDC subject 形態

GitHub Actions 一加 `environment:` 就會把 OIDC sub claim 從 `ref:...` 改成 `environment:<name>`。**WIF binding 必須對齊**，否則 token exchange 直接 403。文件留個 comment 解釋為何用 environment subject，避免下次有人不知為何而改回。

### 5. Foundational stack 變更走「先本機 apply 再 PR」

`ci/github-actions-wif` 標 foundational tag，CI 不會碰它（chicken-and-egg 防護）。改 WIF 流程：
1. 本機個人 ADC 先 apply 修好
2. push branch + PR + merge 補 source-of-truth

這比正常 PR-first 反向，但對 CI 信任邊界類資源是必要的（不修 binding，CI 連 plan 自己都做不到）。

## SA 架構（Lab 05a 後）

| SA | OIDC subject | 用途 |
|----|-------------|------|
| `github-actions-tofu` | `subject/repo:.../environment:production` | apply (main push, 需 approval) |
| `github-actions-tofu-plan` | `subject/repo:.../pull_request` | PR plan (read-only) |
| `github-actions-tofu-drift` | `subject/repo:.../ref:refs/heads/main` | drift detection (schedule + workflow_dispatch, 不經 environment) |

## 後續

- [ ] Lab 05b（暫定）：用 `script` block 把 `tofu init/plan/apply/drift` 包成 `terramate script run`，CI + 本機共用 entry
- [ ] Lab 05c（若有需要）：Catalyst components 把 network module 升級成 versioned blueprint
- [ ] 等下次有 dev/* stack 真實變更時，可考慮把 `stacks/dev/network/moved.tf` 與 `network.tm.hcl` 內的測試註解清掉（separate cleanup PR）
