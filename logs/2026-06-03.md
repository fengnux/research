# 2026-06-03 實驗日誌

## 目標

重新規劃 [`evidence-pack-foundation`](../audit/labs/evidence-pack-foundation.md) 的執行環境：新的 DuckDB / Trivy / GCS pipeline 不在本機跑，改用專用 Compute VM `evidence-runner`。

## 決策

原本有兩個候選：

- 沿用 `dev-vm`：既有 IAP / NAT / stop-start runbook 可直接用
- 新開 `evidence-runner`：多一個 VM stack，但身份與用途乾淨

最後採用 **專用 `evidence-runner`**。

理由：

- `dev-vm` 已偏 K8s / demo bastion，用途跟 evidence pipeline 不同
- evidence pipeline 需要讀寫 evidence bucket，若加在 `dev-vm` SA 上會混入 container 權限
- `evidence-runner` 可以只綁 bucket `objectViewer` + `objectCreator`
- VM attached service account 可透過 metadata server 取得短期 token，符合 ADR-002 不用 long-lived credential 的方向

## 實作規劃落地

`tofu-terramate-lab` 新增 `stacks/evidence/runner/`：

- `google_service_account.evidence_runner`
- `google_storage_bucket_iam_member.evidence_runner_reader`
- `google_storage_bucket_iam_member.evidence_runner_writer`
- `google_compute_instance.evidence_runner`

VM 設計：

- `e2-micro` / Debian 12 / 10GB `pd-standard`
- `asia-east1-b`
- 無 public IP
- IAP SSH，沿用 tag `iap-ssh`
- attached SA：`evidence-runner@research-lab-495809.iam.gserviceaccount.com`

`research` 更新：

- foundation lab 改成在 `evidence-runner` 上安裝 DuckDB / Trivy
- DuckDB wrapper token 來源改為 `gcloud auth print-access-token`
- 新增 [`evidence-runner-instance`](../audit/labs/evidence-runner-instance.md) runbook
- `audit/README.md` 補 runner 入口

## 驗收待辦

- [ ] `terramate run --tags evidence-runner -- tofu plan`
- [ ] apply 後確認 VM 無 external IP
- [ ] IAP SSH 進 VM
- [ ] VM 內 `gcloud auth print-access-token` 可取得 attached SA token
- [ ] `gsutil cp` 可寫入 evidence bucket
- [ ] DuckDB 可讀 `sarif/2026-06/*.sarif.json`
- [ ] 跑出第一份 `audit/artifacts/2026-06/sarif_summary.md`

## 後續

- `demo-vm-evidence-pack.md` 仍保留，但它是 demo/bastion 用途；foundation 實驗改由 `evidence-runner` 承接
- foundation 完成後再決定是否把 runner startup/tool install 流程自動化
